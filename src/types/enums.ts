export enum ColorCodes {
    RED = 31,
    GREEN = 32,
    YELLOW = 33,
    BLUE = 34
}

export enum Types {
    ATTRIBUTE = "attribute",
    METHOD = "method"
}

export enum ArgTypes {
    CALLBACK = "callback",
    CONTENT = "content",
    BOOLEAN = "boolean",
    UNDEFINED = "undefined"
}
